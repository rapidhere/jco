group 'ranttu.rapid.jco'
version '1.0-SNAPSHOT'

dependencies {
    compile project(':jco-core')

    compile group: 'com.google.auto.service', name: 'auto-service', version: '1.0-rc4'
    compile group: 'com.google.auto', name: 'auto-common', version: '0.10'

    // add dependency for tools.jar
    compile files("${System.getProperty('java.home')}/../lib/tools.jar")
}